{# \src\PMM\PlatformBundle\Resources\views\Advert\menu.html.twig
Le template (dans le layout general de l'appli) des menus (couche 3) #}

<ul class="nav nav-pills nav-stacked">
	{% for advert in listAdverts %}
		<li>
			<a href="{{ path('pmm_platform_view', {'id': advert.id}) }}">
				{{ advert.title }}
			</a>
		</li>
	{% endfor %}
</ul>